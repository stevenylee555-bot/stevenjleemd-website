// Site-wide constants. Single source of truth for values referenced across
// many pages and components.

// Dr. Lee's ZocDoc booking profile. Every "Book" call to action links here, so
// this is the site's primary conversion endpoint. Sourced from the "Schedule
// Online" button on his live site (stevenjleemd.com); confirm it still resolves
// to his profile before launch, since a wrong value breaks every booking link.
export const ZOCDOC_URL = "https://www.zocdoc.com/doctor/steven-j-lee-md-77795";
